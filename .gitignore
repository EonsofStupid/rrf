/target
**/*.rs.bk
Cargo.lock.bak

# Model weights / caches — DevPULSE artifacts live outside the tree
/models
/weights
*.safetensors
*.gguf
*.onnx

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Runtime estate + event stream (was committed by accident as .rrf/)
/.rro/
/.rrf/
*.jsonl.tmp

# real benchmark corpora (downloaded, not authored here)
/eval-data/
